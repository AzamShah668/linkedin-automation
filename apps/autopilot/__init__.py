"""Job Hunt Autopilot — the Python runtime that replaces the Claude-Code-as-server stack.

See docs/knowledge/22-rewrite-architecture.md. The rule everything follows from:
anything that is the same every time -> code; anything different every time -> AI.
"""
